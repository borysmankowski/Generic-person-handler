services:
  postgres-test:
    image: postgres:latest
    container_name: postgres-test
    ports:
      - "5436:5432"
    environment:
      POSTGRES_DB: db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin

